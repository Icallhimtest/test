<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="module_category_tournament" model="ir.module.category">
        <field name="name">Tournament</field>
        <field name="sequence">15</field>
    </record>

    <record id="group_tournament_user" model="res.groups">
        <field name="name">Participant</field>
        <field name="implied_ids" eval="[(4, ref('base.group_user'))]"/>
        <field name="category_id" ref="module_category_tournament"/>
    </record>

    <record id="group_tournament_manager" model="res.groups">
        <field name="name">Organiser</field>
        <field name="implied_ids" eval="[(4, ref('group_tournament_user'))]"/>
        <field name="category_id" ref="module_category_tournament"/>
        <field name="users" eval="[(4, ref('base.user_root'))]"/>
    </record>
<!-- 
    <record id="group_use_sla" model="res.groups">
        <field name="name">Show SLA Policies</field>
        <field name="category_id" ref="base.module_category_hidden"/>
    </record> -->

    <record id="base.default_user" model="res.users">
        <field name="groups_id" eval="[(4, ref('group_tournament_user'))]"/>
    </record>

    <data noupdate="1">
        <!-- Manager gets all team access rights --><!-- 
        <record id="tournament_manager_rule" model="ir.rule">
            <field name="name">Tournament Organiser </field>
            <field name="model_id" ref="model_tournament"/>
            <field name="domain_force">[(1,'=',1)]</field>
            <field name="groups" eval="[(4, ref('group_tournament_manager'))]"/>
        </record> -->
        <!-- user only gets to read his own teams (or open teams) -->
        <!-- <record id="helpdesk_user_rule" model="ir.rule">
            <field name="name">Helpdesk User</field>
            <field name="model_id" ref="model_helpdesk_team"/>
            <field name="domain_force">['|', ('member_ids','in', user.id), ('member_ids','=', False)]</field>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="False"/>
            <field name="perm_create" eval="False"/>
            <field name="perm_unlink" eval="False"/>
            <field name="groups" eval="[(4, ref('group_helpdesk_user'))]"/>
        </record> -->
        <!-- Split by company on tickets, teams and SLAs -->
        <!-- <record id="helpdesk_ticket_company_rule" model="ir.rule">
            <field name="name">Project: multi-company</field>
            <field name="model_id" ref="model_helpdesk_ticket"/>
            <field name="global" eval="True"/>
            <field name="domain_force">['|',
                                            ('company_id', '=', False),
                                            ('company_id', 'child_of', [user.company_id.id]),
                                        ]</field>
        </record>
        <record model="ir.rule">
            <field name="name">Project: multi-company</field>
            <field name="model_id" ref="model_helpdesk_team"/>
            <field name="global" eval="True"/>
            <field name="domain_force">['|',
                                            ('company_id', '=', False),
                                            ('company_id', 'child_of', [user.company_id.id]),
                                        ]</field>
        </record>
        <record id="helpdesk_sla_company_rule" model="ir.rule">
            <field name="name">Project: multi-company</field>
            <field name="model_id" ref="model_helpdesk_sla"/>
            <field name="global" eval="True"/>
            <field name="domain_force">['|',
                                            ('company_id', '=', False),
                                            ('company_id', 'child_of', [user.company_id.id]),
                                        ]</field>
        </record> -->
    </data>
</odoo>
